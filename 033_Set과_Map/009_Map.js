//009 Map
/*
    - Map 객체는 키와 값의 쌍으로 이루어진 컬렉션이다.
    - Map 객체는 객체와 유사하지만 다음과 같은 차이가 있다.
        - 객체는 키로 문자열 또는 심벌 값만 가능하지만, Map은 객체를 포함한 모든 값이 가능하다.
        - 객체는 이터러블이 아니지만, Map은 이터러블이다.
        - 객체는 요소 개수 확인을 Object.keys(obj).length로 찾지만, Map은 Map.prototype.size 메서드로 찾는다.
 */