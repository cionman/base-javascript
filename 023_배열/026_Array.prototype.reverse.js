// 026 Array.prototype.reverse
// 원본 변경

// 원본 배열의 순서를 뒤집으며, 원본 배열또한 변경되고, 반환값 또한 변경된 배열이다.
const arr = [1, 2, 3];
const result = arr.reverse();

console.log(arr); // [ 3, 2, 1 ]
console.log(result); // [ 3, 2, 1 ]
